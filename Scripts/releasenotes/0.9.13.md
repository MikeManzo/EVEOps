- EVEOps now keeps an encrypted backup of your characters in the Keychain and restores them automatically if its local data is reset after a crash or update. You can also export and import a passphrase-protected backup from Settings > Accounts.
